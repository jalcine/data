/**
 * File providing compile-time data about runtime-configuration.
 */

#ifndef __CONFIG_HPP__
#define __CONFIG_HPP__

#cmakedefine DEBUG

#define WNTRDATA_DATA_DIR "@WNTRDATA_DATA_DIR@"
#define WNTRDATA_LING_DIR "@WNTRDATA_LING_DIR@"
#define WNTRDATA_ONTO_DIR "@WNTRDATA_ONTO_DIR@"

#include <string>

using namespace std;

using std::string;

namespace Wintermute {
    namespace Data {
        /**
         * @brief
         * @class Configuration config.hpp "config.hpp"
         */
        class Configuration {
        public:
            /**
             * @brief
             * @fn getDirectory
             * @return const string
             */
            static const string getDirectory(){ return WNTRDATA_DATA_DIR;}
            /**
             * @brief
             * @fn Initialize
             */
            static void Initialize();
            /**
             * @brief
             * @fn Deinitialize
             */
            static void Deinitialize();
        };
    }
}

#endif // __CONFIG_HPP__

